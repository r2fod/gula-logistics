# Pendientes

_Actualizado tras dos sesiones de trabajo en paralelo (Gemini/Antigravity + Claude), verificando en vivo lo que hizo cada uno (19-20/09/2026)._

## Resuelto el 20/09/2026 (auditoría completa de la app)

- ~~**Resumen Financiero por semana, mes y año**~~ — pedido por el usuario el 21/09 ("para mayor control"). La pestaña tiene un selector **Semana / Mes / Año / Todo** con flechas para ir al periodo anterior o siguiente (no deja avanzar a uno que no ha empezado). La **semana es la operativa, de martes a lunes** (el lunes es la cola de la semana anterior), así "Semana 3" del planning y la del resumen son la misma; mes y año son los naturales; un turno cuenta en el periodo en que EMPIEZA (la boda del sábado que acaba de madrugada no se parte). Abre en la semana que está abierta arriba. Totales, coste por trabajador, desglose por evento y gráficas se calculan solo con los turnos del periodo; los **pax** de cada evento son los de las semanas de ese periodo (en la vista de una semana, un evento que se repite en dos semanas trae los pax de esa semana, no la suma con otra donde se repite). Módulo `periodosFinancieros.js` + tests; la pestaña calcula ahora sus totales (antes los recibía ya hechos de `PartnerDashboardView`).
- ~~**Semana 3: 62,5 h de jornadas sin tarea caían en "Tareas Internas" y los costes por evento no cuadraban**~~ — el usuario lo pidió otra vez el 21/09 ("arréglalo sin que se pierda la info; las tareas hechas puedes modificarlas para adaptarlo a los costes"). Las 62,5 h eran turnos fichados como "Inicio de Jornada"/jornada sin tareas (sobre todo sábado noche, domingo y lunes). **Arreglo sin tocar ni un fichaje ni el planning** (`repartoPorPlanning.js`): ese tiempo se reparte entre las tareas del planning en las que esa persona estaba asignada — primero las que se solapan con su turno (por minutos de solape), si no las de ese día, si no las de esa semana (por lo que dura cada una) — y solo si no tiene ninguna se queda en "Tareas Internas". Total de horas y de dinero idéntico (y las horas de cada persona sin cambio); solo cambia el reparto por evento, y la pantalla avisa de cuántas horas son estimadas por planning (`≈ 62,5 h`, y por evento). **Límite / a confirmar con el usuario**: es una estimación. Los turnos del domingo de dos conductores (hasta la 01:30) solo tenían asignada en el planning la recogida de una boda pequeña, así que casi todo va a ella; y la devolución del camión de alquiler está como "Solo Domingo 00:45-01:30" (debería ser ya lunes de madrugada). Si el usuario dice qué hicieron de verdad, se ajusta la tarea (asignados/horas) y el reparto cambia solo.
- ~~**Tareas de la semana 3 ya hechas que se veían sin marcar (Grafo & Flujo y progreso de la Vista Pública)**~~ — reportado por el usuario el 21/09 a las 16:3x. Comprobado en Mongo: a las 16:15 las 4 tareas del lunes seguían con `completed:false` (el marcado por hora solo se guarda mientras HAY una sesión de admin abierta, y desde las 10:45 no la había; a las ~16:27 las marcó una sesión de admin del navegador de pruebas al abrir la app, que ya está cerrada). Pero además había un fallo de las vistas: **Grafo & Flujo** solo leía la casilla guardada y a las **bodas del sábado y a las tareas de domingo/lunes ni siquiera les calculaba "hecha"** (una semana entera terminada las seguía enseñando pendientes), y el tile de progreso de la Vista Pública tampoco miraba la hora. **Arreglo**: ambas usan `isTaskEffectivelyDone` (la misma regla que Cuadrante, vista de trabajador y monitor en vivo); test que falla con el código anterior. Consecuencia: ninguna vista depende ya de que haya una sesión de admin abierta para mostrar una tarea pasada como hecha; el `completed` guardado solo hace falta para el desmarcado permanente y las tareas "en proceso". Tras el arreglo, las 40 tareas de la semana 3 constan marcadas.
- ~~**La semana 3 (ya terminada) debe tenerlo todo marcado**~~ — pedido por el usuario el 21/09. Las 40 tareas de la semana 3 ya constaban `completed:true` en Mongo, pero la regla solo cubría tareas con horario que ya pasó: una tarea sin horario o con horario mal escrito de una semana acabada se seguía viendo pendiente. **Arreglo**: `isWeekFinished` (taskPlanning.js) — una semana está terminada desde el lunes de cola, cuando ya no queda ninguna tarea con horario por hacer (+45 min de margen), y nunca si es Borrador o sus fechas son ilegibles; `isTaskEffectivelyDone` da por hecha cualquier tarea de una semana terminada (salvo las desmarcadas a propósito, `reopened`). Además `cerrarSemanasTerminadas` (useWeeks.js) lo guarda en Mongo para TODAS las semanas terminadas, no solo la activa (respeta `reopened` y las tareas con alguien fichado; idempotente). Tests: fallan con el código anterior.
- ~~**Al crear la Semana 4 (borrador) se alteraban los números de la semana 3**~~ — reportado por el usuario el 21/09 ("has jodido la semana 3"). Investigado contra los datos reales: (1) **culpa mía**: el registro de pax juntaba los eventos de TODAS las semanas por nombre, borradores incluidos, y la Semana 4 (con "Evento Encamina" a 55 pax) pisaba los 100 pax que ese evento tenía en la semana 3 en el desglose de costes; además el resolutor de tareas también leía los borradores. **Arreglo**: los borradores ya no cuentan para nada (ni pax ni enlace fichaje→evento), un evento repetido en semanas aceptadas suma sus pax, y el reparto de una tarea de varios eventos usa los pax de SU semana (`buildTaskContextResolver`). (2) **efecto de mi editor**: dos tareas del martes de la semana 3 (recoger sillas y recoger sofá) cambiaron de texto a `"Boda X - Recoger…"` al tocar las etiquetas de evento del editor, que reescribían el texto; cada una tiene un fichaje guardado con el texto ORIGINAL, que quedó desligado de su tarea. **Arreglo**: el editor ya no reescribe el texto de las tareas que llevan el evento en el campo `event` (edita ese campo); y se restauró el texto original de esas dos tareas conservando el evento que el usuario había elegido. Comprobado antes y después: el resto de la semana 3 idéntico (solo esos dos textos y un `reopened` de un desmarcado del usuario). **Lección**: al añadir una función nueva, comprobar qué pasa con lo que YA existe (una semana nueva no debe cambiar los números de las anteriores).
- **Planning automático desde el calendario (21/09) — construido, INERTE hasta configurar Render** — pedido por el usuario: "que de forma automática se anticipe una semana o dos y genere el planning de logística de cada semana", "pueden estar en semana borrador, a vista de revisarlo y aceptarlo para que se active". **Cómo funciona**: (1) el servidor lee el "Calendario Gula" (`GET /api/calendario/eventos`, solo admin; `server/src/services/calendario.js`; NO devuelve `personal`/importes/notas); (2) `client/src/data/weekGenerator.js` (función pura, con tests) convierte los apuntes en un BORRADOR: eventos (boda/comunión/corporativo, con pax, lugar y HORA de inicio → el montaje se calcula hacia atrás desde esa hora), recogidas/devoluciones de alquiler (los alquileres continuos no cuentan), descargas apuntadas como boda (→ descarga adelantada de la boda grande cercana), vacaciones del equipo (nadie se asigna en ellas), reglas de equipo (Kerly/Jose solo limpian, Irene/Raúl no cargan, conductores + apoyo cargan), formato "Evento - Tarea", sin solapes por persona y con AVISOS de todo lo que ha tenido que proponer o no ha podido cubrir; (3) `anticipacion.js` decide qué semanas faltan (las 2 siguientes a la actual) y las guarda con `POST /api/logistics/weeks/draft`, que **nunca pisa nada** (409 si existe; solo reemplaza un borrador si se pide; id determinista `week_auto_AAAA-MM-DD` para que dos sesiones no dupliquen); (4) `App.jsx` lo lanza con sesión de admin (a los 4 s de abrir y cada 30 min, máx. cada 6 h por navegador; silencioso si el calendario no está configurado); (5) la semana en borrador sale marcada "— BORRADOR" en el selector, con un banner (avisos del generador, **Aceptar y activar**, **Regenerar desde el calendario**); un borrador NO es nunca la semana por defecto ni se abre con `?week=` si no eres admin. Además la semana por defecto (sin `?week=`) ya no es siempre la 3: es la que contiene HOY. **Para activarlo, poner en Render**: `CALENDARIO_PROJECT_ID`, `CALENDARIO_API_KEY` y `CALENDARIO_CODIGO` (el código es lo que va detrás de `?cal=` en el enlace del calendario; el proyecto y la clave son la configuración web de su Firebase: consola → Ajustes del proyecto → Tus apps). Son datos que dan acceso de lectura al calendario: solo en Render, nunca en el repo ni en el chat. Probado contra el calendario real: genera las semanas del 22/09, 29/09 y 06/10 sin solapes. **Límites**: los horarios sin hora en el calendario son PROPUESTOS (con aviso); los alquileres sin lugar salen como "Por confirmar"; las semanas sin eventos ni alquileres no crean borrador; no genera los preparativos del lunes que caen en la semana anterior (avisa).
- **Semana 4 (22–27/09): borrador generado por el nuevo generador desde el calendario, pendiente de que el usuario la revise y la acepte** (21/09). Sustituye a un primer borrador hecho a mano que tenía mal el martes: el calendario trae la HORA DE INICIO de los eventos corporativos y el generador monta hacia atrás desde ella. Está como borrador (selector "— BORRADOR", banner con Aceptar/Regenerar), no es la semana por defecto de nadie y la semana 3 no se ha tocado. Los avisos del propio borrador dicen qué se ha propuesto (horas de las bodas, carga del lunes que cae en la semana anterior, devolución de alquiler sin apuntar). Decisiones del usuario: horarios y quién hace qué, en el editor de admin.
- ~~**Costes por evento y persona de la semana 3 (anterior al formato "Evento - Tarea")**~~ — pedido por el usuario el 21/09 ("mira los eventos que había y arregla el planning para tener ajustados los costes por evento y personal, sin romper nada"). **Sin tocar ningún texto de tarea** (los fichajes guardan ese texto y se desligarían): cada tarea y boda lleva un campo NUEVO `event` (p.ej. `"Boda A"`, `"Boda A + Boda B"`, `"Logística Preparación"`), y `buildTaskEventResolver` enlaza cada fichaje con la tarea por su texto (sin distinguir espacios, acentos ni el horario pegado) para sacar su evento; el evento escrito en el texto sigue mandando. Eventos de la semana según el calendario: cinco (tres bodas y dos eventos corporativos) con sus pax registrados en `week.events`; un sexto evento aparece en el planning pero no en el calendario (sin pax). **Aplicado en producción el 21/09** (escritura aditiva de los campos `event` en las 40 entradas y de `events` con los pax; comprobado que, quitando esos campos, la semana quedó idéntica; copia previa hecha antes de escribir — para deshacerlo basta con quitar `event` de las tareas y `events` de la semana). Comprobado en la pantalla real y simulado con los fichajes reales: **231,5 h y 2.475 € antes y después, horas de cada persona idénticas**; solo cambia el reparto por evento (de "Logística Preparación 102 h" a las bodas y eventos por su nombre). **Límite** (resuelto después, ver "62,5 h de jornadas sin tarea"): 51,5 h seguían en "Tareas Internas" — tiempo de jornada fichado sin tarea concreta.
- ~~**El asistente de nueva semana no tenía dónde poner las recogidas y devoluciones de alquiler**~~ — pedido por el usuario el 21/09 ("recogidas de camiones o generador o cosas de alquiler sí hay que tenerlo en cuenta"). Nueva lista "¿Qué recogidas o devoluciones de alquiler hay?" (día, hora, qué) que llega al prompt de Gemini como tareas de su día, con una sola persona y bajo el evento "Logística Preparación" salvo que sean de un evento concreto. Con tests de prompt y de componente.
- ~~**Una tarea que es de DOS eventos (ej. recoger material en Dealde para dos bodas) no tenía cómo expresarse**~~ — preguntado por el usuario el 21/09 (captura del editor con el evento "Boda Joaquín y Maria y Boda Rocio", que en los costes habría salido como una tercera "boda"). **Ahora**: los eventos van separados por `" + "` antes del guion (`"Boda A + Boda B - Recoger material Dealde"`; también se entiende la forma `"Boda A y Boda B"` que ya estaba escrita), y **sus horas y su coste se reparten a partes iguales entre los eventos** (los totales no cambian: `summarizeByEvent`, `data/eventSummary.js`, con tests). En el editor de admin, chips tocables con los eventos de la semana (y las tres categorías) para añadir/quitar eventos de una tarea sin escribir; el planning enseña una etiqueta por evento; Gemini recibe la regla y, si una tarea sin evento nombra a varios eventos conocidos, se unen con `+`. **Reparto por PAX (confirmado por el usuario el 21/09: "si la boda pesa más es más por los pax")**: cada boda/evento lleva su número de pax en `week.events = [{ name, pax }]`; una tarea de varios eventos reparte horas y coste en proporción a sus pax (`getEventShares`), y **si a alguno le falta el pax, a partes iguales** (no se inventa un peso). Los pax se anotan al crear la semana (campo "Pax" de cada evento del asistente, que también se los pasa a Gemini para dimensionar personal y camiones) y, en semanas ya creadas, en el editor de admin (desplegable "Pax por boda/evento"); el desglose de costes los muestra junto al evento. **Pendiente del usuario**: anotar los pax de las bodas de la semana en curso desde el editor (aún no hay ninguno, así que ahora reparte por igual).
- ~~**Tareas que se ven marcadas sin estar hechas y "no deja desmarcar"**~~ — reportado por el usuario el 21/09 a las 10:03 (captura: Sofá 09:00-09:30 y Generador 09:30-10:00 tachadas). Comprobado en Mongo: ambas seguían con `completed:false`. Eran **tres fallos a la vez**: (1) lo que se VEÍA hecho usaba el margen 0 (tachada al acabar su hora exacta) mientras lo que se GUARDABA esperaba 45 min; (2) el clic actuaba sobre el dato suelto, no sobre lo que se veía: pulsar una tarea tachada por la hora la MARCABA de verdad (`false→true`) en vez de desmarcarla; (3) aunque se desmarcara, a los 45 min el reloj la volvía a marcar sola. **Arreglo**: `isTaskEffectivelyDone` (`taskPlanning.js`) es la única regla para verla, para el clic y para guardar: hecha si está marcada, o si pasó su hora + 45 min y nadie la desmarcó a propósito. **Desmarcar deja `reopened:true`** (persistido por el `PATCH`, que ahora acepta `completed` + `reopened` y nada más): el reloj ya no la vuelve a marcar; marcarla de nuevo (o fichar su salida) lo quita. **Nunca se autocompleta una tarea "en proceso"**: si alguien tiene un turno abierto sobre ella (`taskRef`), se salta (`getInProgressTaskKeys`; los turnos abiertos de más de 16 h no cuentan). Las tareas de flota (camión) solo muestran su casilla. Tests: 149 en cliente (incluye el hook `useWeeks` con la captura como caso), 20 en servidor. Verificado en el navegador con los datos de la mañana del lunes. **Ojo**: el desmarcado se guarda de verdad solo cuando Render tenga el servidor con este cambio; hasta entonces el cliente lo respeta en local pero el siguiente sondeo puede traer la tarea sin `reopened`.
- [ ] **Revisar el horario de "Devolución Camión Albacar (Alquiler)"** — el 21/09 quedó con `00:45-01:30` y "Solo Domingo": eso es la madrugada del domingo, ya pasada, así que el reloj la dará por hecha sola. ¿Era `12:45-13:30`? También "Lunes — Limpieza de vajilla…" (sin día fijado) sale con `completed:true` desde antes de que llegara su hora: si hace falta se desmarca (ahora ya no vuelve a marcarse).
- [ ] **Decidir si el reloj debe marcar tareas por horario**: se mantiene (pedido el 20/09, con margen de 45 min) pero con desmarcado permanente y protección de "en proceso". Si aun así molesta, la alternativa es quitar el marcado por hora y dejar solo el marcado manual / al fichar salida — cambio pequeño (`autoCompletePastTasks` y `isTaskEffectivelyDone`).
- ~~**El planning no usaba nombres de evento, así que los costes "por evento" salían como 31 líneas de tareas sueltas**~~ — pedido por el usuario el 21/09 con capturas de su hoja "Planing Eventos" (columnas Fecha / Evento / Tarea; eventos "Boda X y Y", "Logística Preparación", "Logística Carga", "Limpieza Eventos") y su "Dashboard Personal/Eventos". La app ya tenía la convención `"Evento - Tarea"` (el editor de admin: "Nombre del Evento") y el Resumen Financiero ya agrupaba por lo que va antes del guion, pero la IA escribía textos libres ("Recoger Sillas Carvillo — 90 sillas…") y las rayas largas se tomaban como separador: cada tarea era su propio "evento". **Comprobado con los fichajes reales: 31 "eventos" distintos → 8** (Logística Preparación, Tareas Internas, Logística Carga, Evento SUOT, tres bodas, Limpieza Eventos), mismas horas totales. **Arreglo** (`data/eventNaming.js`, un solo sitio para toda la app): separador solo el guion normal entre espacios; sin evento explícito se infiere lo seguro (fichajes "Boda: Finca (Camión)" → "Boda Finca", textos que empiezan por "Boda X"/"Evento X", limpieza → Limpieza Eventos, carga → Logística Carga, recogidas/devoluciones/preparación/supervisión/descarga → Logística Preparación) y lo demás se queda como estaba. **Generación**: el prompt de Gemini exige `"EVENTO - Tarea"` con los nombres EXACTOS de los eventos que el usuario añadió en el asistente ("Boda Finca Norte") o las tres categorías, con ejemplos al estilo de la hoja, y si la IA se salta el formato se completa después (`normalizeGeneratedEvents`). **Planning**: el evento se ve como etiqueta en Cuadrante y en la vista de trabajador; el editor de admin sugiere los eventos ya usados. La tabla de costes se llama ahora "Desglose por Evento". **Límites**: lo ya fichado sin evento explícito se agrupa por palabras clave (aproximado: p.ej. "descarga y montaje" de una boda cae en Logística Preparación porque el fichaje no decía de qué boda); las semanas nuevas saldrán exactas. Las bodas del sábado siguen fichándose como "Boda: lugar (camión)".
- ~~**CRÍTICO: "Generar Planificación" sacaba los eventos de la semana anterior**~~ — reportado por el usuario el 21/09 (captura: "Sot de Chera", "Mas dels Refranys", "Evento Especial 3"). Causa: **sin clave de Gemini en ese dispositivo** (la clave vive solo en el `localStorage` de cada navegador, así que el móvil no la tenía) `generateScheduleWithGemini` devolvía una **demo fija** con fincas y tareas de otra semana, sin ningún aviso, y la pantalla dejaba pulsar "Crear la Semana con esta Planificación": la semana nueva se creaba con datos inventados. Lo mismo tras cualquier error de la API. Además el código llamaba a `gemini-1.5-flash`, un modelo ya retirado, así que aun con clave fallaba y caía en la misma demo. **Arreglo**: se elimina la demo; sin clave o con error devuelve `null` + un mensaje claro y no se ofrece crear nada; modelo `gemini-2.5-flash` (con `gemini-flash-latest` si no existe); la clave va en cabecera, no en la URL; se pide `application/json` y se valida la forma de la respuesta; el asistente de nueva semana tiene ahora un campo para pegar la clave (se guarda en ese navegador) y el aviso se desplaza a la vista en móvil. Comprobado en el navegador (sin clave: aviso, sin botón de crear) y con tests. **Pendiente del usuario**: pegar la clave de Gemini una vez en cada dispositivo desde el que quiera generar semanas (se saca gratis en aistudio.google.com/apikey); **sigue sin probarse una generación real** contra Gemini.
- ~~**El asistente de "Crear Nueva Semana" solo preguntaba "¿cuántas bodas hay el sábado?"**~~ — pedido por el usuario el 21/09: hay una boda el viernes y dos eventos el martes, y no tenían dónde ir. Ahora se añade una lista de **bodas y eventos con su día** (Martes–Lunes, con el número real según el rango de fechas escrito: "Martes 22"), tipo (Boda/Evento), lugar y horario opcional, y el prompt de Gemini (`buildWeekPrompt` en `geminiScheduleService.js`, con tests) pide planificar para cada uno la carga, la ruta, el montaje y la recogida en el día que toque; los de sábado van a `saturdaySpecial.weddings`, los demás como tareas de su día. Además la IA ya recibe la regla de poner `targetDay` ("Domingo"/"Lunes") en las tareas de la lista compartida, para que la semana generada no salga con tareas ambiguas. Comprobado en el navegador a 375 px (sin desbordes) y con tests de componente. **Sin probar contra Gemini de verdad** (necesita la clave del usuario): revisar el resultado de la primera semana generada antes de aplicarla.
- ~~**Avisos de "Alguien más ha guardado cambios en esta semana" sin que nadie hubiera guardado nada**~~ — reportado por el usuario el 21/09 (captura del aviso al editar desde el móvil). Causa: una sesión de admin con el estado **desactualizado** repetía el mismo `PATCH /weeks/:id/tasks` cada 20 s para siempre (el efecto del sondeo de `App.jsx` se monta una vez y llamaba siempre a la `autoCompletePastTasks` de la primera render, que veía pendientes tareas ya completadas), y cada PATCH subía `updatedAt` sin cambiar nada, así que el guardado de cualquier admin chocaba con el control de concurrencia. Visto en vivo: `updatedAt` avanzaba cada 20 s exactos con el contenido idéntico. **Arreglo doble**: el servidor no escribe si la tarea ya está en ese estado (el PATCH es idempotente, cubre también a las pestañas viejas que sigan abiertas) y el cliente llama a la última versión de `autoCompletePastTasks` vía `ref`.
- ~~**Se podía fichar horas antes de empezar (ej. lunes de madrugada, con tareas de las 09:00)**~~ — pedido por el usuario el 21/09: "no debería estar habilitado hasta 5 minutos antes de la siguiente tarea" y, aclarado, **"no en cada tarea, sino la primera tarea de la jornada"** (y en jornada partida, la primera del siguiente tramo). La puerta se aplica siempre que se EMPIEZA un turno (no hay turno activo): el primero del día y también la vuelta de una jornada partida (tras la pausa, la primera tarea del siguiente tramo); entonces "Iniciar jornada" y los "Fichar esta tarea" esperan hasta 5 min antes de la primera tarea ("Espera 7 h 10 min para fichar" / "Podrás fichar a partir de las 08:55"); con un turno en curso se puede cambiar de tarea sin esperas. Antes se comprobaba por día de la semana suelto (una tarea de lunes sin etiquetar se podía fichar de madrugada; cualquier tarea de una semana futura también). Nuevos `getNextTaskStart` (próxima ocurrencia que no ha terminado: una tarea sin etiquetar a las 14:00 del domingo con hora 09:00 es la del lunes) e `isTaskTooEarlyToStart`. "Siguiente tarea" = la que EMPIEZA antes entre las no completadas ni terminadas (antes una boda de sábado ya pasada se quedaba fijada). `isDayInFuture` por fecha real. Además, el botón de iniciar jornada muestra una frase distinta al azar cada vez que se abre la vista ("¡A por el día! Iniciar jornada", "¡Arrancamos!…") — pedido del usuario. **Ojo**: una tarea sin "Día Específico" cuenta como lunes al decidir si está terminada; etiquetar las de domingo evita esperas raras.
- ~~**CRÍTICO: tareas marcadas como hechas sin estarlo (domingo/lunes, y riesgo para cualquier semana nueva)**~~ — reportado por el usuario ("algunas sin estar hechas las marcó"; "no debería marcar las de domingo, están en proceso"; "¿en qué quedó lo de las devoluciones del lunes?"; "el planning no deja diferencia entre domingo y lunes"). Causas, las tres reales: (1) domingo y lunes comparten UNA lista (`sundayMonday.tasks`) y una tarea sin `targetDay` se evaluaba siempre como DOMINGO — las devoluciones del lunes se dieron por hechas el domingo; (2) `isTaskChronologicallyPast` comparaba solo el DÍA DE LA SEMANA, sin saber a qué semana del calendario pertenece el planning: una semana nueva (p.ej. la del 22–27) tenía martes–sábado "ya pasados" desde el minuto uno y `autoCompletePastTasks` los habría guardado como hechos en Mongo; (3) una tarea sin horario "HH:MM - HH:MM" se daba por hecha en cuanto acababa el día. **Arreglo**: `taskPlanning.js` resuelve la FECHA REAL de cada tarea a partir de `meta.dateRange` (`parseWeekRange`, `resolveTaskDate`, `getTaskPastStatus`, `isTaskPast`); sin `targetDay` una tarea de la lista compartida cuenta como **lunes** (el último día posible: mejor pendiente de más que hecha sin estarlo); sin horario utilizable o con `dateRange` ilegible NO se marca ni se tacha nada; `autoCompletePastTasks` solo escribe con `=== true`. Todos los llamadores usan lo mismo (`useWeeks`, `WorkerView`, `LiveMonitorPanel`, `ScheduleTab`). Además, para la semana siguiente: una semana **clonada o generada por IA ya no hereda los "completada"** de la anterior (`clearWeekCompletion`), el asistente añade el año al rango (`ensureYearInDateRange`) y avisa si las fechas no se entienden, "Aplicar Gemini" ya no pisa `dateRange` con el texto de relleno de la IA, y los números de día de `WorkerView`/`TaskFlowGraphView` (estaban escritos a mano: "Lunes 14"… "Domingo 20", `3 Bodas Simultáneas`) salen ahora de las fechas de la semana. **Planning**: el bloque domingo/lunes de `ScheduleTab` se separa en "Domingo 20" / "Lunes 21" / "Sin día fijado" según el "Día Específico" de cada tarea; en `WorkerView` la pestaña LUN ya no enseña las tareas etiquetadas "Solo Domingo" (por eso aparecía la recogida de Refranys bajo "Lunes") y cada tarea etiquetada lleva su chip. Tests: 95 en cliente (incluye simulación con las fechas y horas reales del 20–21/09 y un test de componente de `ScheduleTab`).
- ~~**Seguridad: `POST /api/notifications/notify` usaba un middleware de admin propio, con secreto hardcodeado en el repo público**~~ — encontrado en una auditoría general pedida por el usuario. `notifications.routes.js` tenía su propio `requireAdmin` casero que comparaba el header `Authorization` contra `process.env.ADMIN_TOKEN || 'gula_admin_secret_2024'` — un secreto de fallback escrito literalmente en el código, visible para cualquiera en GitHub, completamente aparte del sistema real de sesión (JWT firmado + revocación por `tokenVersion`) que usa el resto de la app. Además, **la función estaba rota de verdad**: el cliente (`pushService.js`) manda el token JWT real de admin (`getStoredAdminToken()`), que nunca coincide con ese string fijo — así que enviar una notificación push real habría fallado siempre con 401, aunque el secreto estuviera bien configurado en Render. Corregido sustituyendo el middleware casero por el `requireAdmin` real y compartido (`middleware/requireAdmin.js`) — mismo que protege el resto de rutas de admin. Con tests actualizados (antes probaban literalmente el secreto hardcodeado como si fuera el comportamiento correcto).
- **Investigado y descartado (no era un bug)**: sospecha de que `findOneAndUpdate` con un payload parcial sin `$set` (visto en `balances.routes.js`, `clock.routes.js`) reemplazaría el documento COMPLETO en Mongo en vez de actualizar solo esos campos — la regla clásica de MongoDB. Verificado a fondo (código fuente de Mongoose y del driver + fichajes borrados reales en producción, que conservan todos sus campos pese a haberse escrito de verdad): Mongoose envuelve automáticamente los campos sueltos en `$set` antes de mandarlos ("fix up $set sugar" en `castUpdate.js`), así que esto nunca ha sido un problema real. Se dejó un `$set` explícito en `balances.routes.js` de todas formas (más claro, protege además contra un `id` suelto en el payload que compitiera con el de la URL) — sin cambio de comportamiento, solo más explícito.
- ~~Borrar/restaurar un fichaje (papelera) fallaba en silencio si el guardado real no llegaba al servidor~~ — mismo hueco que ya se había tapado para editar fichajes/saldos, pero `deleteClockEntryInAPI`/`restoreClockEntryInAPI` se habían quedado fuera de aquel arreglo. Ninguna de las dos comprobaba `res.ok` (ni siquiera distinguían un 401 por sesión caducada de un éxito — `fetch` no lanza excepción por un status de error), y `useClockings.js` las llamaba sin `await` ni comprobar nada. El admin veía el fichaje "movido a la papelera" (o "restaurado") al instante, pero si de verdad no se guardó, volvía a aparecer solo en el siguiente refresco de 20s sin explicación. Corregido: ambas funciones devuelven ahora si la petición fue bien, y `handleDeleteClockEntry`/`handleRestoreClockEntry` avisan con el mismo `alert()` que ya usan el resto de escrituras cuando falla de verdad.
- ~~**`AdminWorkerEditorModal.jsx` no comprobaba nombres duplicados al añadir a alguien**~~ — encontrado en la auditoría. Como el id de la ficha financiera se deriva del nombre (`name.toLowerCase().replace(/\s+/g,'-')`), dos personas con el mismo nombre (o un reintento tras una errata) habrían colisionado sobre la MISMA ficha de Saldos en Mongo — y `handleRemoveWorker` filtra por nombre exacto, así que quitar a cualquiera de las dos las habría borrado a ambas del roster de golpe. Corregido con un aviso si ya existe alguien con ese nombre (sin distinguir mayúsculas/tildes de espacio) antes de guardar.
- ~~**"Eventos Clave" (pestaña Logística) tenía la lista de tareas a mostrar fijada a mano (`['j1','j2']`) y se desincronizaba**~~ — encontrado en la auditoría y confirmado ya manifestándose en producción: existe una tarea real `j3` ("Recogida Evento TOUS y descarga en Restaurante") añadida después, que nunca llegó a aparecer en este panel por no estar en esa lista fija. Corregido para reconocer cualquier id con el patrón `j<número>` en vez de una lista cerrada — no hace falta volver a tocar este archivo cada vez que se añada una tarea "clave" nueva el jueves.
- ~~Riesgo de crash en el Grafo (`TaskFlowGraphView.jsx`) si una tarea llegaba sin campo `.text`~~ — encontrado en la auditoría (no reproducido con datos reales actuales, pero real de todas formas: una tarea generada mal por IA o editada a mano en Mongo sin `text` habría hecho `undefined.includes(...)` y roto el render completo del Grafo, no solo esa tarea). Corregido con un guard defensivo.

- ~~**CRÍTICO**: `autoCompletePastTasks` marcó las 7 tareas de domingo/lunes como completadas en Mongo de madrugada, antes de que llegara su hora~~ — reportado por el usuario ("no les aparecen las tareas de domingo"). Causa: `isTaskChronologicallyPast` aplicaba el ajuste de "estamos de madrugada, sumar 24h" a CUALQUIER tarea del día, no solo a las que de verdad cruzan medianoche — una tarea normal de tarde comprobada a las 02:00 se marcaba pasada por error. Corregido de raíz + **revertidas a mano las 7 tareas reales en Mongo** a `completed:false`, verificado en vivo que un ciclo de polling posterior ya no las vuelve a corromper.
- ~~**El botón de iniciar jornada no comprobaba bien el margen de 5min antes de la hora de la tarea**~~ — el margen SÍ existía (en "INICIAR JORNADA AHORA", el flujo real de fichaje por jornada completa), pero con un bug: calculaba la hora de la tarea "inmediata" siempre sobre la fecha de HOY sin comprobar que esa tarea fuera de hoy — si la primera tarea pendiente de la semana era de un día futuro, podía dejar iniciar jornada antes de tiempo. Unificado en `isTaskTooEarlyToClockIn` (taskPlanning.js), que sí comprueba el día real. De paso arreglado un bug de campo suelto en las bodas de sábado (`w.time` en vez de `w.timeFrame`, nunca existía de verdad).
- ~~**CRÍTICO: el wraparound domingo→lunes de `isTaskChronologicallyPast` estaba roto de raíz**~~ — encontrado el 20/09 investigando el pendiente de `targetDay` (ver 🟡 más abajo). `weekDayOrder` pone 'domingo' como ÚLTIMO elemento (ordinal 6, el más alto de la semana) — al comprobar una tarea de domingo ya en lunes, la condición `ordinal > todayOrdinal` (6 > 0) cortaba y devolvía "día futuro" ANTES de llegar a la lógica de "ayer exactamente" que gestiona el cruce de medianoche. Con sábado→domingo (ordinales 5 y 6) no pasaba, por eso ese caso sí estaba cubierto por tests y este no se detectó antes. **Efecto real**: una boda de domingo que cruza medianoche (ej. 20:30-00:30) nunca se marcaba pasada una vez que de verdad era lunes, ni fichando el lunes por la tarde — se quedaba "en curso" para siempre. Corregido moviendo el cálculo de "ayer exactamente" antes del corte de "día futuro". Con 4 tests nuevos que reproducen el caso exacto (`taskPlanning.test.js`) y confirman que sábado→domingo sigue funcionando igual.
  - Corregir ESTE bug por sí solo, sin lo de abajo, habría creado un problema nuevo — ver el pendiente de `targetDay` justo debajo, que se resolvió a la vez por eso mismo.

- ~~Las tareas pasadas no se guardaban como completadas de verdad, solo se veían tachadas en pantalla~~ — pedido por el usuario. Nueva función `autoCompletePastTasks` en `useWeeks.js`: recorre todas las tareas de la semana activa (días normales, domingo/lunes, bodas de sábado) y marca como completada de verdad en Mongo cualquiera que ya pasó su horario con margen de sobra. Se dispara desde una sesión de admin real, al abrir la app y en cada tick del polling de 20s ya existente — nunca desde el móvil de un trabajador, para no disparar la misma comprobación 8 veces a la vez.
  - **Margen de 45 min** (`TASK_COMPLETION_GRACE_MINUTES`, compartido entre el guardado y "Actividad en Tiempo Real"): confirmado con el usuario tras un caso real (Johan marcado "completado" 9 min después de la hora prevista de fin de una boda, mientras seguía trabajando). Sin margen, cualquier evento que se alargue un poco de lo estimado se marcaría como terminado antes de tiempo.
  - Arreglado también el caso de turnos que cruzan medianoche (ej. boda 20:30-00:30): antes de este cambio, en cuanto pasaba la medianoche se daba por pasada la tarea del día anterior sin mirar si su horario real seguía vigente. Ahora se compara en la misma línea de tiempo continua.
  - `markTaskCompleted` no actualizaba `lastLocalEditRef` como sí hace `toggleTask` — el polling de 20s podía traer de vuelta datos del servidor de antes de que el guardado llegara y desmarcar la tarea recién completada. Corregido (el usuario lo preguntó explícitamente antes de que se desplegara).
  - El endpoint `PATCH /weeks/:weekId/tasks` del servidor no soportaba `dayKey === 'sabado'` (las bodas viven en `saturdaySpecial.weddings`, no en `schedule.sabado`) — marcar una boda como completada devolvía 404 en silencio. Corregido con test de cobertura.

## Resuelto el 19/09/2026 (sesión de Gemini + verificación/fixes de Claude)

- ~~Toggle de tareas no persistía~~ — el polling de 20s pisaba el cambio local con datos viejos del servidor antes de que el PATCH llegara. Corregido con una ventana de gracia de 5s (`lastLocalEditRef` en `useWeeks.js`) que evita refrescar desde el servidor justo después de un cambio local.
- ~~`.env.local` apuntaba al servidor local sin MongoDB~~ — eliminado, causaba que Saldos & Acuerdos se viera "oscuro"/vacío en local.
- ~~Redondeo de horas fichadas a la hora completa (bug de dinero)~~ — un cambio de Gemini (`7b0a82e`) redondeaba `durationHours` al entero más cercano (`Math.round(rawDuration)`), pagando de más o de menos en cualquier turno que no cayera justo en una hora exacta. Corregido a redondeo a la **media hora** más cercana (`Math.round(rawDuration*2)/2`, confirmado con el usuario: por debajo de 15min de la siguiente media hora baja, desde ahí sube — así se paga siempre en billete de 5€/10€, nunca en céntimos). Con tests.
- ~~`POST /api/logistics/optimize` ("Optimizar y Limpiar Base de Datos" en Ajustes Admin) no borraba nunca nada~~ — filtraba por `isDeleted: true`, pero el campo real del schema de `ClockEntry` es `deleted`. Corregido.
- ~~`POST /api/logistics/upload-rental` sin límite de tamaño ni tipo de archivo~~ — cualquier admin podía subir cualquier archivo de cualquier tamaño al servidor. Ahora solo PDF, máx 10MB, con errores devueltos en JSON (no había error-handler global en Express).
- ~~Saldos & Acuerdos crasheaba al abrir la pestaña (pantalla en negro) y al guardar cualquier cambio~~ — la modularización de `PartnerDashboardView` en pestañas separadas (`7b0a82e`) dejó dos referencias colgando: `handleSendWhatsApp` (usada pero solo definida en el hijo `TeamBalancesTab.jsx`) y `setSavingBalanceId` (misma historia). El build no lo detectaba por ser errores de JS en tiempo de ejecución, no de sintaxis — encontrados con un barrido `eslint --rule no-undef` sobre todo `client/src`, que quedó limpio tras los arreglos. **Esta clase de bug (referencias colgando tras un refactor grande) es justo la que hay que barrer con esa misma técnica después de cualquier reorganización de componentes.**
- ~~`pairShiftsFromEntries`/`aggregateShiftsByWorker` sin memoizar~~ — se recalculaban en cada render; en `LiveMonitorPanel.jsx` esto pasaba 60 veces por minuto por el cronómetro de 1s, sin ningún fichaje nuevo. Memoizado con `useMemo` en `LiveMonitorPanel`, `WorkerView` y `PartnerDashboardView`.
- ~~`AdminTaskEditorModal.jsx` reimplementaba a mano el caso especial domingo/lunes en 6 funciones~~ — exactamente la duplicación que `taskPlanning.js` se creó para eliminar. Unificado (con `'sundayMonday'` como alias reconocido de `'domingo'` en `taskPlanning.js`, ya que ese editor usa el nombre real del campo de Mongo en vez del dayKey semántico que usa el resto de la app).
- ~~`@dnd-kit` importado, configurado (sensores, handler de drag-end) pero nunca conectado al JSX~~ — código muerto desde que se escribió (el reordenado real siempre fue por los botones ⬆⬇). Era la única dependencia del proyecto que usaba esa librería — eliminada. Bundle: 869KB → 853KB.
- ~~Bolsa Mensual de Jefferson empezaba en 0 horas consumidas en cada render, ignorando lo ya persistido~~ — corregido en `TeamBalancesTab.jsx`, arranca desde `purseInfo.consumedHours`.
- ~~**Jefferson aparecía con +0,00€ y sin desglose en su propia tarjeta de Saldos**, a pesar de tener 872,59€ y su bolsa de 80h bien guardados en Mongo~~ — encontrado al verificar el fix de la Bolsa Mensual en producción con datos reales (comparando la respuesta de `/api/balances` contra lo que se veía en pantalla). El merge en `PartnerDashboardView.jsx:167` (`mergedBalancesData`, el que da a `TeamBalancesTab`) comparaba nombres de forma exacta (`"jeferson" === "jefferson gula"` → false), a diferencia de `findWorkerHours` un poco más abajo en el mismo archivo, que ya tenía la normalización correcta (`ff`→`f` + prefijo) para este mismo problema de nombres. Al no encontrar coincidencia, se generaba una tarjeta en blanco con `id: "jeferson"` en vez de usar el registro real (`id: "jefferson"`). Unificado con la misma normalización.
- ~~**"Actividad / Tarea Asignada" salía en blanco en Actividad en Tiempo Real** para cualquiera fichado en un turno genérico ("JORNADA") y asignado a una boda de sábado en curso o próxima~~ — reportado por el usuario con captura real (Gonzalo, sábado 23:07, boda "Partida Paraíso" en curso de 20:30 a 00:30). Las bodas (`saturdaySpecial.weddings`) no tienen campo `.text` como las tareas normales (tienen `location`/`truck`), y `getWorkerTaskInfo` en `LiveMonitorPanel.jsx` asumía que todo objeto de tarea lo tenía — devolvía `undefined`. Corregido con un helper (`getTaskDisplayText`) que también evita un crash latente: si una boda tenía el array `assigned` vacío, el filtrado por texto libre intentaba `undefined.toLowerCase()` y rompía.

## Resuelto en sesión anterior (15/09/2026, dejado aquí solo como referencia)

- ~~Render no desplegaba el servidor~~ — resuelto, deploy manual confirmado y funcionando.
- ~~POST /api/logistics/weeks sin autenticación~~ — resuelto, ahora exige admin; el trabajador sigue pudiendo autocompletar su tarea vía un endpoint nuevo y estrecho (PATCH /weeks/:weekId/tasks) que solo toca `completed` de una tarea.
- ~~Auto-edición/borrado de fichaje propio nunca funcionaba de verdad~~ — resuelto, se quitó ese botón de la vista del trabajador.
- ~~Jaime (novato) va solo en la boda de María y Joaquín~~ — resuelto por eliminación: Jaime se quitó del roster, del planning de Semana 3, y de todo el código (ejemplos, fallbacks, comprobaciones sueltas) donde quedaba mencionado. La regla de "novato acompañado" ya no aplica.
- ~~El Grafo (🕸️ Grafo & Flujo) mostraba trabajadores y camiones de una lista fija escrita en el código~~ — resuelto: deriva siempre de los datos reales (`workersList` / `activeWeekData.trucks`).
- ~~El editor de tareas podía fallar en silencio al guardar~~ — parcialmente resuelto: `updateWeeks` avisa con un mensaje claro si el guardado real falla (p.ej. sesión de admin caducada), en vez de fallar callado.
- ~~Filtro "Trabajador" del Cuadrante resaltaba por texto mencionado, no por `assigned[]` real~~ — resuelto, ahora compara contra la asignación real de cada tarea.
- ~~Bug de medianoche en Actividad en Tiempo Real~~ — resuelto: las tareas con horario que cruza medianoche (ej. "22:00 - 00:00") ahora sí se detectan como "en curso" durante la noche. Encontrado hoy al verificar un hallazgo de Gemini antes de darlo por bueno.
- ~~Botones del Informe de Fichajes sin proporcionar / formato de hora mezclado~~ — resuelto, ver detalle en `MEJORAS.md`.
- ~~Cualquiera sin sesión veía el panel completo (Cuadrante/Saldos) si instalaba la app o entraba directo a la URL base~~ — resuelto: sin sesión real de admin ni identidad de trabajador reconocida, ahora se muestra `PublicView` (sin saldos ni nóminas). Reportado por el usuario tras instalar la app desde un enlace compartido.
- ~~Un trabajador podía ver el Grafo completo de TODO el equipo (todas las tareas, todos los camiones, todos los compañeros) desde su propia vista~~ — resuelto: `TaskFlowGraphView` dentro de `WorkerView` ahora se restringe solo a las tareas/días/camiones realmente conectados a ese trabajador, vía la prop `restrictToWorkerName`. El Grafo del panel de admin sigue mostrando todo, sin cambios. Reportado por el usuario ("puede ver cosas que no debería ver").
- ~~Un admin quedaba atrapado al probar el enlace de un trabajador (ej. tras añadir a "Dan") y parecía que ese trabajador había desaparecido~~ — resuelto en cuatro pasadas: (1) la persistencia de identidad de trabajador ya no se guarda/restaura en un dispositivo con token de admin válido; (2)+(3) el botón para volver a Admin ya no depende del nombre visto (antes solo con "Raúl") ni de si ese dispositivo concreto ya tenía sesión guardada — ahora se muestra siempre; (4) a petición del usuario, con sesión de admin sigue siendo un botón normal ("👑 Panel Admin"), pero sin sesión es deliberadamente discreto (un enlace de texto pequeño, no un botón) para no invitar a un trabajador cualquiera a tocarlo, y si cancela el login sin escribir la contraseña se queda exactamente donde estaba (antes perdía su vista igualmente al cancelar). Verificado en el navegador los cuatro casos. Dan nunca se borró — seguía en el roster (9 miembros) en todo momento; el susto era solo quedarse sin salida visible.
- Nuevo: botones de Fichar deshabilitados si el día de la tarea aún no ha llegado.
- Nuevo: horas reales fichadas visibles también en las tarjetas de Saldos & Acuerdos.
- Nuevo: se puede quitar/añadir trabajadores del roster desde la propia app (antes solo se podía añadir) — **ojo, ver limitación real más abajo**.
- Nuevo: 61 tests unitarios (antes no había ninguno) para la lógica que ya causó bugs reales.

## 🔴 Alto impacto / riesgo real — pendiente

- [ ] **Poner en Render las variables del calendario** (`CALENDARIO_PROJECT_ID`, `CALENDARIO_API_KEY`, `CALENDARIO_CODIGO`; ver `server/src/services/calendario.js`). Render ya desplegó el servidor nuevo (21/09 ~19:40: `POST /api/logistics/weeks/draft` y `GET /api/calendario/estado` devuelven 401 en vez de 404), pero sin esas variables la lectura del calendario y los borradores automáticos siguen apagados. Recomendado usar el código de solo mirar (`?ver=`), no el de editar (`?cal=`). Después: comprobar `/api/calendario/estado` con sesión de admin y crear los borradores de las semanas siguientes.
- [ ] **El repositorio es PÚBLICO y su HISTORIAL conserva datos sensibles** (revisado el 21/09; el árbol actual está limpio: sin claves, sin `.env`, `balancesData.js` es una plantilla vacía). En los 430 commits del historial hay: (1) **saldos y acuerdos reales de 8-9 personas con nombre** en `client/src/data/balancesData.js` (commits del 14-15/09; se limpió del árbol al día siguiente pero sigue en el historial); (2) una **clave privada VAPID** (43 caracteres) en `server/.env.example` (18/09; quitada el mismo día) — **comprobado el 21/09: las alertas push NO están configuradas en producción** (el bundle publicado no lleva la clave pública `VITE_VAPID_PUBLIC_KEY` y GitHub Actions no tiene ninguna variable), así que esta clave no da acceso a nada hoy; aun así, dala por comprometida y **genera una nueva** (`npx web-push generate-vapid-keys`, sin pegarla en ningún chat ni commit) cuando actives las alertas: se pone en Render (`VAPID_PUBLIC_KEY`/`VAPID_PRIVATE_KEY`) y como variable de Actions (`VITE_VAPID_PUBLIC_KEY`, que el workflow todavía no pasa al build); (3) tres componentes antiguos con contraseñas escritas en el código (`AdminPanel.jsx`, `LoginModal.jsx`, `SecureAccessModal.jsx`, ya borrados; no se pudo comprobar su contenido) — asumir que esas contraseñas están expuestas y **cambiar la clave de admin** desde "Clave". **Opciones**: purgar el historial (`git filter-repo` + force-push: destructivo, hay que autorizarlo, y los clones/forks ya hechos conservan lo publicado) o pasar el repo a privado (GitHub Pages en repos privados exige plan de pago). **Nada de esto se ha hecho.**
- [ ] **Datos personales que siguen en el árbol actual** (menos graves): nombres del equipo con su tarifa por hora (`rate` 10/14 €/h) en `App.jsx` y `useWorkers.js`; nombres reales de fincas y proveedores como opciones de `ClockInModal.jsx` y ejemplos de `GeminiAssistantModal.jsx`; nombres de clientes y del equipo en `CONTEXTO.md`/`MEJORAS.md`. El 21/09 se sustituyeron por nombres ficticios los que se habían colado en los tests y textos nuevos. Decidir si se generaliza el resto (las tarifas y las opciones de `ClockInModal` son datos que la app usa: habría que moverlos a Mongo).
- ~~Fichajes offline se pierden para siempre, sin aviso ni reintento~~ — resuelto. Cola persistente de pendientes (`gula_pending_clock_sync_v1`) en `apiService.js`: si `saveClockEntryToAPI` falla, el fichaje se encola en vez de perderse; `fetchClockEntriesFromAPI` ahora fusiona esa cola con lo que traiga el servidor en vez de sobrescribir a ciegas; `retryPendingClockEntries()` reintenta activamente cada 20s (para todos, no solo admin) y al instante en cuanto el móvil recupera cobertura (evento `online`). Para que un reintento nunca duplique un fichaje que en realidad SÍ se guardó la primera vez (solo que la respuesta no llegó sin cobertura), `POST /api/clock` en el servidor ahora es idempotente por `id` (índice único ya existente en Mongo): un `id` repetido devuelve 200 con el documento existente en vez de un 500 genérico. Con tests en ambos lados (cliente y servidor).
  - **Nota**: solo cubre el "aviso" a nivel técnico (consola, no UI). No se ha añadido un indicador visible en pantalla tipo "N fichajes pendientes de sincronizar" — si se quiere, es una extensión sencilla sobre `getPendingClockEntriesSnapshot()`.
- ~~`POST /api/clock` acepta `rate`/`earnings`/`durationHours` tal cual los mande el cliente, sin validar~~ — resuelto parcialmente. `earnings`/`durationHours` (valores calculados, el flujo real nunca los manda) se descartan siempre en el servidor. `rate` sí es legítimo que lo mande el cliente (hay una función real de tarifa personalizada por fichaje, `AdminClockEditModal.jsx`), así que en vez de prohibirlo se acota a un rango razonable (0-100€/h) — bloquea el ataque obvio (un rate inventado tipo 1000€/h) sin romper la función real. **No** se implementó derivar `rate` del roster en el servidor como decía el arreglo original: el roster de trabajadores solo vive en `localStorage` del navegador (ver el punto de abajo sobre eso), no existe todavía en Mongo como para que el servidor pueda consultarlo — eso requeriría primero migrar el roster a Mongo, que es un cambio más grande aparte.
- ~~El token de admin puede filtrarse por la cabecera `Referer` al compartir el enlace de socias por WhatsApp~~ — resuelto: `<meta name="referrer" content="no-referrer">` en `index.html` (ningún enlace saliente de la app, ej. los de Google Maps de las tareas, manda ya la URL como Referer) + limpieza de `token`/`key` de la barra de direcciones con `history.replaceState` en cuanto se guarda en `localStorage` (solo esos dos parámetros, no toda la URL, para no perder otros como `?week=` en el mismo enlace).
- ~~Turnos "zombi" sin tope de duración si se olvida fichar salida~~ — resuelto parcialmente. La facturación YA estaba topada a 14h desde antes (`pairShiftsFromEntries`, `isAnomalous`) — lo que quedaba sin cubrir era el turno TODAVÍA ABIERTO (sin fichar salida): se mostraba como "EN TURNO" indefinidamente, con un cronómetro que podía llegar a días. Nueva función `isZombieShift` (`shiftCalculations.js`, umbral 16h) — en Actividad en Tiempo Real, un turno abierto de más de 16h cambia de la insignia verde "EN TURNO" a una ámbar "⚠️ REVISAR" (con tooltip). No se cierra solo — sigue haciendo falta que Admin lo cierre a mano desde el editor de fichajes, esto solo lo señala en vez de dejarlo pasar desapercibido.
- ~~Riesgo de colisión de ID en fichajes casi simultáneos~~ — resuelto. Los 8 sitios donde se generaba `id: Date.now().toString()` (ClockInModal, WorkerView, LiveMonitorPanel, AdminClockEditModal, y el fallback del servidor en clock.routes.js) ahora usan `crypto.randomUUID()`.
- ~~**"Actividad / Tarea Asignada" mostraba la próxima tarea del día aunque el trabajador no hubiera fichado entrada todavía**~~ — reportado por el usuario con captura (Gonzalo en "DESCANSO" mostrando "Devolución Sofá" como si ya estuviera en ello). La selección de tarea era correcta (la más próxima por hora), pero no debía mostrarse en absoluto sin fichar — parecía que ya estaba trabajando en algo que ni había empezado. Ahora, sin fichar entrada, la caja muestra "⏸️ Sin fichar aún — sin tarea en curso" en vez de adivinar una tarea. Aplica igual a todos los trabajadores (es un cambio en la lógica común, no algo específico de Gonzalo).

## 🟡 Impacto medio — pendiente

- [ ] **Traer los eventos del "Calendario Gula" al asistente de nueva semana** — idea del usuario (21/09): que la planificación se haga viendo los eventos ya apuntados en su calendario (`https://r2fod.github.io/Generate_Checklist_Gula/calendario/?cal=<id>`, otra app suya, datos en la nube). Se ha comprobado que el enlace se lee sin sesión y que cada evento trae día, tipo (Boda/Corporativo), lugar y pax; también camiones y "tareas/visitas". **Qué importar (reglas del usuario, 21/09)**: los EVENTOS (tipos Boda y Corporativo) y las RECOGIDAS/DEVOLUCIONES de camiones, generadores y material de alquiler (sí hay que tenerlas en cuenta); se ignoran las visitas, reuniones, pruebas de menú y demás "tarea/visita", y los "día cerrado/completo". Ojo: "Camión Covey" NO es una tarea diaria, es un alquiler continuo (9/09 → devolución el 19/10) que se repite en cada día: es disponibilidad de camión, no una recogida; solo cuenta el día de la devolución. El calendario se carga con un código de SOLO LECTURA (`?cal=`) contra su nube (Firestore, documentos `indice/*`; es una suite con más módulos): leerlo desde Gula implicaría copiar su configuración de nube, entrar con acceso anónimo y depender de su esquema. **Sin construir**: hoy los eventos se añaden a mano en el asistente. Decidir cómo importarlos (leer el calendario desde la app —acopla los dos proyectos y sus reglas de la nube— o exportar/pegar un texto). Al importar: evitar duplicados (en el calendario hay bodas del mismo día y lugar con nombres casi iguales, y algún apunte repetido), y usar el campo pax, no el número del nombre (un evento cuyo nombre dice "25 pax" tiene 55 anotados). **Nota de seguridad menor**: las dos apps viven en `r2fod.github.io` (mismo origen), así que comparten `localStorage`, incluida la sesión de admin de Gula Logística; código de terceros en cualquiera de las dos podría leerla.
- ~~Bolsa Mensual: los turnos fichados automáticamente no contaban las horas de bolsa ya consumidas a mano~~ — **ya estaba resuelto** (commit `d7a4a46`, 19/09 22:45, esta entrada había quedado desactualizada). `TeamBalancesTab.jsx:296` arranca `consumedBolsa` desde `worker.purseInfo?.consumedHours` real, no desde `0`. Confirmado leyendo el código vigente el 20/09.
- ~~Riesgo de doble contabilidad entre turnos manuales y fichados automáticamente~~ — mitigado con un aviso (no se puede bloquear del todo: a veces un concepto manual el mismo día es legítimo aparte, ej. una rotura o un adelanto, y solo un humano sabe distinguirlo). Al añadir un turno manual en Saldos & Acuerdos, si ya hay fichajes reales de ese trabajador ese mismo día, se muestra "⚠️ Ya hay fichajes reales... revisa el desglose antes de guardar" antes de confirmar.
- ~~Borrar un concepto de "bolsa de horas" no revierte `purseInfo.consumedHours`~~ — **confirmado** (Gemini tenía razón, verificado línea a línea en `handleDeleteConcept`, `TeamBalancesTab.jsx`). La línea "Valor Acumulado Horas Bolsa" es única (se sobrescribe, no se acumula una por turno) — si se borraba, `purseInfo.consumedHours`/`consumedValue`/`shifts` se quedaban con el valor viejo para siempre, desconectados del desglose. Ahora, al borrar específicamente esa línea, se resetea también el `purseInfo` del trabajador a cero — el resto de conceptos (ej. "Extra tras bolsa") no tocan `purseInfo`, así que borrarlos ya funcionaba bien.
- ~~`apiService.js` tragándose errores en silencio en el resto de escrituras~~ — resuelto para las dos ediciones deliberadas de dinero/datos: `updateClockEntryInAPI` (editar un fichaje desde admin) y `saveWorkerBalanceToAPI` (cualquier cambio de saldo) ahora devuelven `null` en vez de un echo del dato enviado cuando el guardado real falla, y sus llamadores (`persistWorkerBalance`, `handleUpdateClockEntry`) avisan con el mismo `alert()` que ya usaba `saveWeeksToAPI`. Decisión tomada para las otras dos: `saveClockEntryToAPI` ya no necesita alertar (falla → se encola para reintentar, ver el fix de fichajes offline) y `patchTaskCompletionInAPI` (toggle/auto-completar tarea) se deja sin alertar a propósito — se autocorrige solo en el siguiente refresco, y alertar ahí sería muy ruidoso viniendo de `autoCompletePastTasks` marcando varias tareas seguidas.
- ~~Guardados concurrentes se pisan entre sí~~ — resuelto (control de concurrencia optimista, usando `updatedAt` que ya gestiona Mongoose en vez de `__v`, que no encaja bien con el reemplazo completo de documento que ya hacía este endpoint). `POST /api/logistics/weeks` ahora compara el `updatedAt` que trae cada semana del payload (el mismo que el cliente recibió al cargarla, sin tocar) contra el actual en Mongo — si no coincide, rechaza esa semana con 409 sin sobrescribirla (las demás semanas del mismo payload sí se guardan). El cliente (`updateWeeks`) distingue este caso de un fallo cualquiera: no reintenta a ciegas, refresca con la versión real del servidor y avisa para repetir el cambio a mano. Tras un guardado bien hecho, el cliente sincroniza el `updatedAt` fresco para que el siguiente guardado de la misma sesión no se autoconflictúe contra su propio cambio anterior.
  - Verificado con tests (servidor: caso sin conflicto, con conflicto, semana nueva sin `updatedAt` previo; cliente: los tres casos de respuesta de `saveWeeksToAPI`) — 22/22 tests en verde tras el último despliegue. Confirmado por `curl` directo contra Render que el resto de fixes de servidor de esta noche (validación de `rate`, fichaje idempotente, PATCH de sábado) ya están desplegados y funcionando, así que este mismo endpoint también lo está. **No se ha probado el 409 en vivo contra producción real** (dos sesiones editando la misma semana a la vez): `POST /weeks` exige token de Admin válido, que no tengo en este entorno, y al ser un reemplazo del documento completo de la semana no es un endpoint con el que improvisar pruebas en datos reales. La cobertura de tests (que ejercita la ruta real de Express + el middleware real, solo con Mongo mockeado) se considera suficiente por ahora; si quieres la confirmación 100% en vivo, ábrelo en dos pestañas con tu sesión de admin, guarda en una y confirma que la otra avisa del conflicto al intentar guardar después.
- [ ] **Inconsistencia de nombre "Jefferson" vs "Jeferson"** — **el usuario confirmó (20/09) que el nombre correcto es "Jeferson" (una sola f)**. Comentarios del código actualizados para reflejarlo (ya no quedaba ningún "Jefferson" con doble f fuera de comentarios — no había ninguna cadena de código real con ese typo). **Queda un solo paso, que no he podido hacer yo**: el registro de Saldos en Mongo (`WorkerBalance`, `id: "jefferson"`) todavía tiene `name: "Jefferson Gula"` — hay que cambiarlo a `"Jeferson Gula"`. No hay ninguna pantalla en la app para editar ese campo, y `PUT /api/balances/:id` exige token de Admin que no tengo en este entorno. **Cómo hacerlo tú**: en MongoDB Atlas → colección `workerbalances` → documento con `id: "jefferson"` → cambiar el campo `name` de `"Jefferson Gula"` a `"Jeferson Gula"` → guardar. El campo `id` (`"jefferson"`) se deja tal cual — no depende de esa "f" de más, y cambiarlo obligaría a tocar más sitios sin ningún beneficio real. No urge: la normalización `ff→f` en `PartnerDashboardView.jsx` sigue puenteando el cruce mientras tanto, sin ningún bug visible.
- [ ] **El roster de trabajadores (añadir/quitar) solo vive en `localStorage` de cada navegador** — no sincroniza entre dispositivos. Arreglo de fondo: migrarlo a Mongo, como ya se hizo con el planning.
- ~~Fichajes de alguien quitado del roster desaparecían de los totales sin aviso~~ — resuelto. `aggregateShiftsByWorker` solo creaba un "cajón" (bucket) por cada trabajador de `workersList` actual, y descartaba en silencio (`if (!bucket) return;`) cualquier turno de alguien ya no está ahí — sus horas y coste históricos desaparecían de golpe de Resumen Financiero y Saldos & Acuerdos en cuanto lo quitabas del equipo. Ahora, un turno sin bucket se crea uno sobre la marcha con los datos que ya trae el propio fichaje (tarifa, si es nómina), marcado `isOrphaned: true` y con el rol "Ya no está en el equipo" — no hace falta tocar la UI para que sus totales vuelvan a sumar donde ya se mostraban (Resumen Financiero, Informe de Fichajes). Con test que reproduce el caso exacto que antes se daba por bueno (había un test que literalmente esperaba que el turno "huérfano" desapareciera — corregido para esperar lo contrario).
- [ ] **`taskRef` referencia tareas por posición en el array (`{dayKey, taskIndex}`), no por un id estable** — **confirmado** (verificado el 20/09: `taskRef` se captura al fichar entrada en `WorkerView.jsx` y se usa tal cual al fichar salida en `useClockings.js:38` → `markTaskCompleted(dayKey, taskIndex)`; si Admin reordena las tareas de ese día mientras el turno sigue abierto, se completaría la tarea equivocada). **No lo he arreglado todavía**: comprobado contra la semana real en Mongo, **13 de las 34 tareas actuales (38%) no tienen `id`** (sobre todo en miércoles/jueves/viernes y la boda de Joaquín y María), así que migrar `taskRef` a usar `id` en vez de índice primero necesita rellenar esos ids a mano — no hay sistema de auto-generación (regla de `CLAUDE.md`: id corta manual por día, sin chocar con las del mismo día). Es un cambio de más alcance del que parecía, mejor hacerlo aparte y no a medias dentro de esta pasada.
- ~~Filtro "Trabajador" del Informe de Fichajes no aplicaba a las 4 tarjetas resumen~~ — resuelto (`PayrollReportModal.jsx`). Gasto Extras / Horas Extras / Valoración Nóminas / Trabajadores en Turno y el contador de "jornadas completadas" seguían mostrando el total de todo el equipo aunque se filtrara por un trabajador en el desplegable; ahora se calculan sobre el mismo `workerBalances`/`activeWorkerShifts` ya filtrados que usan las tablas de abajo. Verificado con datos de prueba reales (`pairShiftsFromEntries`/`aggregateShiftsByWorker` con dos trabajadores, uno con turno abierto): los totales filtrados suman exactamente al total sin filtrar, y el contador de "activos" distingue correctamente quién tiene turno abierto.
- ~~Pestaña "Estimado (Planning)" no se incluía en el texto de "Copiar WhatsApp"~~ — resuelto, añadida como sección aparte al final del texto copiado, con el mismo aviso que ya tiene la pestaña en pantalla ("no son fichajes reales") para no confundirla con las cifras de arriba. De paso, el listado de turnos del texto copiado (`shifts.forEach`) también usaba la lista sin filtrar en vez de `filteredShifts` — mismo tipo de bug que el de las tarjetas, corregido a la vez por estar en la misma función.
- ~~**Render no había desplegado el servidor nuevo tras ~25 min**~~ — resuelto el 21/09: tardó más de lo normal pero ya sirve las dos versiones nuevas del `PATCH` (idempotente y con `reopened`; comprobado). Sigue sin avisar a GitHub de sus despliegues, así que conviene comprobarlo a mano tras cada cambio de servidor.
- [ ] **El bloque `team` (tarjetas de equipo de la Vista Pública) no tiene pantalla de edición**: vive en Mongo y solo se cambia con `POST /weeks` (admin) o en Atlas. El 21/09 se cambiaron a petición del usuario los textos de Raúl ("Jefe de Logística y Software Developer"), Irene (sin "NO carga ni descarga", + "Metre eventos") y Gonzalo/Ricardo/Johan ("Conductores, carga y descarga"). Si se va a tocar más a menudo, conviene un pequeño editor en el modal de admin.
- [ ] **Regla "Irene y Raúl no cargan ni descargan" — decidir si sigue vigente**: el usuario pidió (21/09) quitar "NO carga ni descarga" de las tarjetas de equipo (Raúl, Irene) y añadir a Gonzalo/Ricardo/Johan "conductores, carga y descarga". Sigue escrita como regla en `CONTEXTO.md` (nº 2) y en el prompt de Gemini (`geminiScheduleService.js`: "NO los asignes bajo ningún concepto"), y Gemini seguiría sin asignarles carga. No la he tocado porque no me lo pidieron; si ya no es cierta, hay que cambiarla en los dos sitios.
- [ ] **"Día Específico" en la lista domingo/lunes**: el usuario ya lo puso en casi todas (21/09); queda "Lunes — Limpieza de vajilla…" sin día fijado (cuenta como lunes) y una tarea nueva sin `id`. Para la semana siguiente: recordar poner el día al crear tareas en esa lista.
- [ ] **`LiveMonitorPanel` mira las tareas del día de HOY de la semana activa**: si se activa una semana que no es la actual (p.ej. la del 22–27 mientras aún es la del 15–20) enseñaría tareas de esa semana como si fueran de hoy. Sin daño en datos; conviene filtrar por fecha real como el resto.
- [ ] **Aviso de React en `PartnerDashboardView.jsx` (`<option key={w.id}>` del selector de semana): "Each child in a list should have a unique key"** — visto en consola del dev. Sin comprobar si es cosmético o si alguna semana llega sin `id`; mirarlo al tocar ese selector.
- [ ] Ricardo tiene dos recogidas la misma mañana del martes (Dealde + apoyo en Albacar con Johan) — posible solape, sin confirmar si es intencional.
- ~~`LiveMonitorPanel.jsx`'s `getAssignedTasksForWorker` no resolvía el alias domingo/lunes~~ — resuelto. En lunes buscaba las tareas asignadas a cada trabajador en `getTaskListForDay(activeWeekData, 'lunes')`, que devuelve `schedule.lunes.tasks` (no existe) en vez de usar el alias a 'domingo'. Ahora, cuando hoy es lunes, se pasa `'domingo'` a `getTaskListForDay` (igual que hace el resto de la app), y el chequeo de "¿ya pasó su hora?" también resuelve el día real por tarea (`targetDay`, mismo patrón que `autoCompletePastTasks`). Verificado con datos reales: antes devolvía 0 tareas en lunes, ahora encuentra la tarea real de Kerly/Jose correctamente. Nota: para una tarea de domingo sin `targetDay` (como la de Kerly/Jose), el comportamiento de "ya pasó su margen" en lunes sigue siendo el mismo ya aceptado contigo (si no se terminó antes del domingo 15:45, se considera pasada el resto del lunes también) — esto no es nuevo, solo ahora se evalúa con el dato correcto en vez de caer siempre al texto genérico por el bug de arriba.
- ~~`autoCompletePastTasks` (useWeeks.js) no miraba el `targetDay` de cada tarea de domingo/lunes~~ — resuelto, pero con un hallazgo más grave por el camino (ver el bug de arriba). Al investigar esto se encontró que el propio wraparound domingo→lunes de `isTaskChronologicallyPast` estaba roto de raíz (ver bug 🔴 de arriba, ya corregido) — y **arreglar ESE bug por sí solo habría invertido este problema en vez de resolverlo**: antes, una tarea "Solo Lunes" nunca se autocompletaba; con el wraparound ya arreglado pero sin mirar `targetDay`, se habría autocompletado en Mongo desde el minuto 0 del lunes, sin haber ni empezado. `autoCompletePastTasks` ahora resuelve el día real de evaluación por tarea (`task.targetDay?.toLowerCase() || 'domingo'`), igual que ya hacía `ScheduleTab.jsx` para pintarla tachada (ese sí ya estaba bien). Verificado con la función real: una tarea 09:00-15:00 con `targetDay:'Lunes'` ya NO se marca pasada ni a las 00:05 ni a las 10:00 del lunes, y sí a las 16:00 (una hora después de terminar). Con tests nuevos en `taskPlanning.test.js` para el propio wraparound.
  - **Corrección (20/09, confirmado por el usuario)**: la tarea real "Lunes — Limpieza de vajilla y utensilios (Kerly + Jose)" (`sl2e`, 09:00-15:00) es en realidad una tarea de **domingo que puede seguir el lunes si no se termina** — el nombre empieza por "Lunes" pero es solo el título, no describe su `targetDay` real. El aviso original de esta entrada (pedir que se le pusiera "Día Específico: Solo Lunes") **era un error**: eso la habría tratado como si empezara de cero el lunes, rompiendo justo el caso real (que pueda seguir en curso del domingo noche al lunes mañana). El estado actual en Mongo (`targetDay` sin poner, evaluado como domingo) ya es el correcto — no hace falta tocar nada. Si no se termina antes del domingo 15:45 (hora de fin + margen de 45min), se autocompletaría sola igual que cualquier otra tarea con margen superado — comportamiento ya aceptado, no algo nuevo de este fix.
  - No se ha tocado `WorkerView.jsx` (tiene su propia copia de `isTaskChronologicallyPast`, ver 🟢 más abajo, ni este fix ni el del wraparound le llegan) ni `LiveMonitorPanel.jsx`'s `getAssignedTasksForWorker` (en lunes busca en `schedule.lunes.tasks`, que no existe — las tareas reales de domingo/lunes no aparecerían en "Actividad en Tiempo Real" ese día, cae al texto genérico por trabajador). Este segundo hallazgo es independiente de todo lo de arriba (no lo empeora ni lo arregla este cambio) — nuevo pendiente añadido abajo.
- ~~Ayuda de transporte (+10€/día): no había forma de excluirla cuando dos trabajadores con esa ayuda comparten coche~~ — pedido por el usuario (Kerly y Jose, mismo coche, solo debe pagarse una vez). Antes, `hasTransportBonus` era solo una insignia decorativa en la tarjeta (ver el campo en el modelo de Mongo) sin ningún efecto real en el saldo — el +10€ real se venía escribiendo a mano, mezclado en el texto libre de un concepto. Ahora, en "Añadir Turno" (Saldos & Acuerdos), si el trabajador tiene la ayuda activada aparece una casilla "Incluir +10€ ayuda transporte este día" (marcada por defecto), que suma o no los 10€ al importe y al concepto guardado. Además, si otro compañero con la misma ayuda ya tiene un concepto con transporte ese mismo día, se muestra un aviso ("🚌 Kerly también tiene transporte añadido ese mismo día...") para recordar desmarcarlo si de verdad compartieron coche — no se bloquea, es información para que decida quien lo guarda. Verificado con datos de prueba replicando el caso real (Kerly con transporte el 21/09, aviso correcto para Jose ese mismo día, sin aviso en otro día; importe con/sin casilla calculado bien en el caso normal y en el de bolsa especial).

## 🟢 Bajo impacto / pulido — pendiente

- ~~La clave de la API de Gemini podía acabar expuesta en el bundle del cliente si se usaba `VITE_GEMINI_API_KEY`~~ — resuelto. Comprobado que no hay ninguna fuga activa hoy (ni en `.env` local ni en el bundle ya desplegado en GitHub Pages, verificado buscando el patrón de clave de Google en el JS real) — era un riesgo preventivo, no un secreto ya filtrado. Quitado igualmente el fallback a `import.meta.env.VITE_GEMINI_API_KEY` en `geminiScheduleService.js` (cualquier variable `VITE_` se compila tal cual en el JS público) — la única clave válida ahora es la que cada admin pega a mano en su propio navegador (persistida solo en su `localStorage`, nunca en el bundle). No se ha construido una ruta propia del servidor para una clave "compartida" porque no hace falta: el patrón actual (clave personal por admin) ya funciona y es el que se usaba de verdad.
- ~~Comprobaciones por nombre de pila hardcodeado en vez de por el campo `isPayroll`~~ — resuelto en los 2 sitios (`shiftCalculations.js`, `PayrollReportModal.jsx`). El `|| workerName === 'Irene' || workerName === 'Raúl'` no era solo un relleno para datos faltantes: con el `||`, forzaba `isSalaried=true` para esos dos nombres **siempre**, aunque su `isPayroll` real dijera lo contrario — si alguno deja de estar en nómina fija algún día, el código lo habría seguido tratando como nómina de todas formas. Confirmado contra los fichajes reales de Irene (4) y Raúl (7) en producción que todos ya traían `isPayroll:true`, así que quitarlo no cambia ninguna cifra actual. Con test que reproduce el caso exacto (antes se daba por bueno el hardcodeo, ahora exige el dato real).
- ~~`WorkerView.jsx` tenía su propia copia de `isTaskChronologicallyPast`, con el bug de medianoche viejo y sin el wraparound domingo→lunes ni `targetDay`~~ — resuelto, sustituida por la versión compartida de `taskPlanning.js`. **Investigando esto se encontró un problema bastante más grave y real**: la pestaña "LUN" de la propia vista del trabajador (selecionable, no solo un adorno) buscaba las tareas en `schedule.lunes.tasks` (no existe) en vez de `sundayMonday.tasks` — **siempre aparecía vacía**, aunque el trabajador tuviera tareas reales de domingo/lunes asignadas. Preguntado al usuario qué debía pasar (¿mostrar lo mismo que "DOM", o quitar la pestaña?) — confirmó que debe mostrar lo mismo que "DOM", ya que es literalmente el mismo dato. Arreglado de forma completa y consistente:
  - `getDayActivities('lunes')` ahora lee de `sundayMonday.tasks` (igual que 'domingo').
  - Nuevos helpers `toStorageDayKey`/`toEvalDayKey`: separan "dónde vive la tarea de verdad" (siempre `domingo`, para fichar/completar/toggle) de "qué día usar para saber si ya pasó su hora" (respeta el `targetDay` propio de la tarea, mismo criterio que `autoCompletePastTasks` y `LiveMonitorPanel.jsx`).
  - `isDayInFuture` tenía el mismo bug de ordinal que ya se corrigió en `taskPlanning.js` (domingo es el último elemento del array, así que comprobado en lunes se leía como "futuro" en vez de "ayer") — mismo fix aplicado aquí.
  - **Evitada la duplicación**: como ahora "LUN" y "DOM" muestran la misma tarea, se excluye 'lunes' de los recuentos totales (`Toda la Semana`, detección de "tarea inmediata" para fichaje de un toque) para no contarla ni mostrarla dos veces — verificado en el navegador: "Toda la Semana (2)" cuenta bien, la tarea de domingo/lunes aparece una sola vez ahí, y aparece correctamente al seleccionar cualquiera de las dos pestañas por separado.
  - Verificado en vivo en el navegador con datos reales (Kerly, `?worker=Kerly`): la pestaña "LUN 14" ahora muestra "Lunes — Limpieza de vajilla y utensilios (Kerly + Jose) (09:00 - 15:00)" con su botón de fichar — antes salía vacía.
- ~~Ningún modal bloqueaba el scroll del fondo en móvil~~ — resuelto. Hook compartido `useBodyScrollLock` (`hooks/useBodyScrollLock.js`, bloquea `document.body.style.overflow` mientras esté abierto, con contador para overlays anidados) conectado en los 10 modales/overlays reales de la app (WeekManagerModal, GeminiAssistantModal, AdminLoginModal, PayrollReportModal, AdminSettingsModal, AdminClockEditModal, AdminWorkerEditorModal, FleetManagerModal, AdminTaskEditorModal, ClockInModal, y los dos overlays propios de PartnerDashboardView — drawer móvil y "Avisar Cambios"). Verificado en el navegador: `overflow: hidden` mientras el menú está abierto, se libera correctamente al cerrar. **Aviso aparte, sin relación con este fix**: durante la propia verificación se pulsó sin querer "Fichar Salida" en la vista real de Kerly (entorno de dev conectado a la API de producción) — se detectó al momento y se revirtió (el fichaje de salida accidental se movió a la papelera vía `DELETE /api/clock/:id`), confirmado que su turno real quedó como estaba antes.
- ~~Claves de `localStorage` para saldos divergentes entre `useBalances.js` (`gula_balances_v1`) y `apiService.js` (`gula_balances_data_v1`)~~ — resuelto, y **no era solo un nombre distinto**: `gula_balances_v1` (la que usaba el estado inicial de la app) solo se actualizaba al añadir un trabajador local — nunca en cada fetch real exitoso —, mientras que `gula_balances_data_v1` sí se mantenía al día en cada `fetchBalancesFromAPI`. Al arrancar la app, antes de que resolviera el primer fetch, se podía mostrar una caché bastante más vieja que la que de verdad estaba al día. Unificado a una sola clave (`gula_balances_data_v1`), reutilizando también el mismo criterio de "¿esto es un dato real o solo la plantilla vacía?" (`hasRealBalancesData`, ahora exportada) en vez de una comprobación parecida pero no idéntica hecha aparte.
- ~~`POST /api/auth/login` sin límite de intentos (rate-limiting)~~ — resuelto. Máximo 10 intentos fallidos por IP en una ventana de 15 minutos; el 11º recibe `429` con cabecera `Retry-After` y un mensaje claro (el cliente ya enseña el `error` del servidor, así que el admin bloqueado ve el motivo en vez de un "contraseña incorrecta" engañoso). Se comprueba **antes** del `bcrypt.compare` (coste 12) para que una IP bloqueada tampoco pueda gastar CPU del servidor; un login correcto borra el contador; una petición sin contraseña (400) no cuenta. Sin dependencias nuevas: contador en memoria (un solo proceso en Render; si se reinicia el contador se pierde, solo da un margen pequeño extra). **Detalle importante**: para que `req.ip` sea la IP real y no la del proxy de Render hubo que activar `trust proxy`, y se activó con `1` (un salto) y **no** con `true` — con `true` Express toma la IP más a la izquierda de `X-Forwarded-For`, que el propio atacante puede falsificar rotándola en cada intento, dejando el límite inútil (con test que reproduce ese intento de evasión). **No verificado en vivo**: que Render tenga exactamente 1 salto de proxy (si hubiera 2, p.ej. Cloudflare delante, `req.ip` sería la IP del borde de Cloudflare y el límite agruparía a varios clientes juntos) — comprobarlo exige provocar un bloqueo real sobre tu propia IP durante 15 minutos, así que no lo he hecho sin avisarte. Con 6 tests nuevos (`auth.routes.test.js`).
- [ ] **Avisos de `npm audit` en el cliente: `esbuild` (moderado) y `vite` (alto)** — revisado con `npm audit` el 20/09 (la nota anterior decía solo "moderada": son 2, una de ellas alta). Ambos afectan **solo al servidor de desarrollo local** (`npm run dev`: permite que una web cualquiera le mande peticiones mientras lo tienes abierto) — no al bundle que se despliega en GitHub Pages, que es estático. Servidor de producción: **0 vulnerabilidades**. Arreglarlo exige `vite` 5.4 → 8.x (`npm audit fix --force`), tres versiones mayores en la herramienta que genera lo que se despliega — no se hace de pasada. Mitigación mientras tanto: no dejar `npm run dev` abierto mientras se navega por webs desconocidas. Si se aborda, hacerlo en una rama aparte comprobando que `npm run build` y el bundle desplegado se ven igual.
- ~~Saldos & Acuerdos accesible sin login de admin si se entra directo a la URL~~ — resuelto: sin sesión real de admin, ahora se muestra `PublicView` (sin saldos ni nóminas) en vez del panel completo. Reportado por el usuario a raíz de instalar la app desde un enlace compartido — el manifest de la PWA pierde los parámetros de la URL al instalarse, así que cualquier enlace acababa cayendo en la vista por defecto.
- ~~`PublicView.jsx` tenía una sección de Checklist muerta que leía `data.tasks` (campo inexistente)~~ — resuelto, y **no era solo código escondido**: dos cosas de esa ruta muerta sí se veían en la vista pública — el tile "Progreso Tareas" (marcaba **0% siempre**) y una pestaña de móvil "Checklist (0)" que llevaba a una sección vacía. Además usaba campos de un modelo antiguo (`task.assignedTo`, `task.priority`) y un `onToggleTask` que los dos llamadores ya pasaban como no-op `() => {}` — nunca fue interactiva. Quitadas la sección, la pestaña y el prop (y los dos `onToggleTask={() => {}}` de `App.jsx`, más los imports que solo usaba esa sección). El tile **se ha dejado con datos reales** en vez de eliminarlo: ahora calcula el progreso desde los días (martes-viernes), sábado (bodas) y domingo/lunes reutilizando `getTaskListForDay`. Verificado en el navegador contra la API real: el tile muestra 97% = 38 de 39 tareas completadas. Si prefieres que la vista pública no enseñe ese dato, basta con quitar el tile.
- [ ] **Imports sin usar en `App.jsx`** (17 avisos de `no-unused-vars` que salieron al pasar el linter: `saveClockEntryToAPI`, `updateClockEntryInAPI`, `getTaskListForDay`, `buildTaskListPatch`, `initialBalancesData`…) — restos de cuando la lógica de fichajes/semanas vivía en este archivo, antes de moverla a los hooks. Inofensivos, solo ruido; limpieza pendiente.

## Funcionalidad pedida, no empezada todavía

- [ ] **Detección de solapes de horario**: avisar si dos tareas asignadas a la misma persona se pisan en el tiempo.
- ~~**Asistente guiado al crear una semana nueva**: preguntar camiones/disponibilidad/bodas y generar la planificación con Gemini~~ — pedido por el usuario. `WeekManagerModal.jsx` sustituye al formulario simple de antes (solo nombre+fechas+clonar): ahora, tras nombre y fechas, pregunta con botones qué camiones hay disponibles (partiendo de los de la semana actual + uno extra a mano), quién del equipo está disponible, cuántas bodas hay el sábado (con detalles opcionales) y notas adicionales — con eso arma un prompt y llama al mismo motor de Gemini que ya usaba el Asistente AI suelto (lógica extraída a `data/geminiScheduleService.js` para no duplicarla entre los dos). Se puede revisar la planificación generada antes de confirmar, o saltarse todo y crear la semana en blanco como antes. Probado en el navegador de punta a punta (sin sesión de Admin real, activando el modal a mano en local): equipo/camiones clonados correctamente de Semana 3, planificación de demostración generada y aplicada, semana nueva creada y puesta como activa. **Sin probar con una API key de Gemini real** — solo con el fallback de demostración que ya existía (mismo camino de código que el Asistente AI suelto, que si funciona con clave real).
- ~~**Gestión de flota (camiones)**: poder añadir/quitar camiones desde la app~~ — hecho por Gemini (`FleetManagerModal.jsx`, 19/09): añadir/quitar camiones por semana, marcarlos propio/alquiler, subir el PDF del contrato de alquiler. Verificado que la subida de PDF está protegida (admin + solo PDF + 10MB tras el fix de hoy); pendiente una revisión visual completa de esta pantalla en sí.
- [ ] **Fase 3 del plan de limpieza — Animaciones**: diferido por decisión tuya.
- [ ] **Revisión de adaptación a pantallas** (móvil/tablet/desktop): solo se ha revisado a fondo un modal (Informe de Fichajes) — el resto de vistas siguen sin repasar sistemáticamente.
- [ ] **Enlaces de trabajador firmados y revocables individualmente**: pedido por el usuario, pero al explicarle que ahora mismo `?worker=Nombre` no lleva ningún token (cualquiera que sepa el nombre puede entrar y fichar como esa persona) se confirmó que **para el caso real que tenía en mente (quitar acceso a alguien, ej. Jaime) ya basta con quitarlo del roster** — `App.jsx` solo activa la vista de un trabajador si su nombre está en el `workersList` actual, así que un enlace antiguo de alguien ya quitado deja de funcionar solo, sin nada que construir. Lo que sigue sin cubrir es un caso más concreto: revocar el enlace de alguien que SIGUE en el equipo (p.ej. si se sospecha que se filtró) sin tener que quitarlo del roster — para eso sí haría falta un token firmado por trabajador (como ya tienen las socias) con posibilidad de regenerar; no se ha construido porque rompería de golpe los enlaces de todo el equipo al desplegarse, y el usuario no ha confirmado que lo quiera todavía.

## Investigado y no reproducido (dejar constancia por si vuelve a pasar)

- [ ] **Salto espontáneo de pestaña en `PartnerDashboardView`**: reportado una vez, investigado a fondo, nunca reproducido. Probablemente un clic accidental.
